---
title: "Pipeline scenarios_bn_pmsi v8 — exécution pas à pas"
output: html_notebook
---

```{r opts, include=FALSE}
# Sécurité : un Knit ne doit JAMAIS lancer le pipeline (heures de calcul).
# Les chunks s'exécutent uniquement au clic (bouton Run), pas au tricotage.
knitr::opts_chunk$set(eval = FALSE)
```

```{r mode_demo, eval=FALSE}
# MODE DÉMO (optionnel) — données FICTIVES et ALÉATOIRES : aucune validité épidémiologique.
# Sur la plateforme : NE PAS exécuter ce chunk. Hors plateforme : l'exécuter AVANT le chunk `session`.
# Il installe un faux paquet pRatihque sur demo/base_demo.sqlite (créée si absente), copie le code dans
# demo/resultats/projet_demo/ (stubs utils/referentiels), active la surcharge du profil « démo » (sorties
# sous demo/resultats/) et pose SCENARIOS_PMSI_DEMO = 1 (le chunk `session` l'affiche). Voir demo/README.md.
source("demo/session_demo.R")
```

Version notebook de `RUN.md` (qui reste la référence texte) pour l'AMONT ; la phase aval
(exploitation du catalogue parquet : repartitionnement, sélection de campagne, palier de débit,
campagnes parallèles, habillage, finalisation) a son propre notebook : `RUN_aval.Rmd`. Fichiers du pipeline :
`config.R` (config + profils), `helpers.R`, `etapes.R` (chaînes base + fonctions
d'étape + `etat_pipeline()`), `extraction.R` (entrée extraction),
`tirage.R` (entrée tirage, **sans base**). Journal : `MODIFICATIONS_V8.md`.

**Usage** : un chunk par étape, dans l'ordre, en lisant les critères de passage entre deux ;
`etat_pipeline()` à tout moment. En cas de plantage : relancer le même chunk, le cache fait le
reste (partiels / refs / chunks déjà écrits sont sautés). Session → Restart R conseillé avant
chaque grosse étape.

---

# 0. Session

Charge config, sources, connexion et les fonctions d'étape SANS rien exécuter
(`SCENARIOS_PMSI_ETAPES_SEULEMENT`). À ré-exécuter après tout Restart R.

Racine du projet : variable d'environnement `SCENARIOS_PMSI_PATH`, sinon `config_locale.R` à la racine du
dépôt (copier `config_locale.exemple.R` ; jamais versionné).

```{r session}
rm(list = ls())
if (nzchar(Sys.getenv("SCENARIOS_PMSI_DEMO"))) {
  cat("################  MODE DÉMO : base fictive (faux pRatihque), sorties sous demo/resultats/ — AUCUNE validité épidémiologique  ################\n")
} else Sys.setenv(SCENARIOS_PMSI_PROFIL = "diagnostic")   # ou "production"
Sys.setenv(SCENARIOS_PMSI_ETAPES_SEULEMENT = "1")
source("extraction.R")   # connecte la base ; pour le tirage seul : source("tirage.R")
etat_pipeline()
```

```{r etat}
etat_pipeline()   # réutilisable entre chaque étape
```

---

# Étape 1 — prep_data

Résolution des besoins (partiels et refs manquants) puis tables temporaires `prep_data_<an>`
des seules années nécessaires. À relancer à chaque nouvelle session d'extraction.

```{r prep_data}
etape_prep_data()          # ans = c(24, 25) pour forcer des années
```

# Étape 2 — séjours courts (AN_REF uniquement)

```{r refs}
etape_refs()               # forcer = TRUE après changement d'AN_REF, de SEUIL_REF_*, de CONVERSION_E669
```

```{r courts}
Sys.setenv(SCENARIOS_PMSI_ETAPES_SEULEMENT = "1"); source("tirage.R")   # famille tirage (sans base)
etape_tirage_courts()      # chunks courts + scenarios_courts_v8_<date>.parquet ; parallélisme : etape_tirage_courts(chunk_range = c(i, j)) par session, plages disjointes, puis un appel final sans plage
etat_pipeline()
```

# Étape 3 — séjours longs

## 3.1 Partiels

```{r partiels}
etape_partiels_longs()     # iterations = data.frame(etbs = "CHR/U", an = 25) pour une itération isolée
```

Lecture des apports, du recouvrement et de la mémoire :

```{r apports}
# Lectures robustes (lire_si_present) : fichier absent = message « produit par <étape> », jamais d'erreur R brute.
apports <- lire_si_present(FICHIER_APPORTS(), "etape_partiels_longs()"); apports   # 90_diagnostics/ [partagé]
if (!is.null(apports)) plot(seq_len(nrow(apports)), cumsum(apports$nb_diag2_nouveaux), type = "b",
                            xlab = "itération (CHR/U 17..26 puis CH 17..26)", ylab = "diag2 nouveaux cumulés")
recouvrement <- lire_si_present(FICHIER_RECOUVREMENT(), "etape_partiels_longs()"); recouvrement
memoire <- lire_si_present(FICHIER_DIAG_MEMOIRE(), "etape_refs() / etape_partiels_longs() / etape_catalogue()")   # 90_diagnostics/diagnostic_memoire_<profil>.csv
if (!is.null(memoire)) memoire[order(-memoire$pic_go), ][1:10, ]
```

## 3.2 DÉCISION de périmètre -> catalogue (sans base)

Éditer `ans` et `etbs` d'après les apports et le recouvrement ; le méta du catalogue en garde
la trace. Le rapport d'extraction mesure l'impact de la conversion E669. Le catalogue est un
**magasin partagé** (`20_catalogue/`) gardé par son `_meta.yaml` : un périmètre différent de la
config est accepté à l'écriture (avertissement) mais le catalogue ne sera chargeable qu'avec
`ANS_HISTORIQUE` / `TYPES_ETBS_LONGS` alignés sur cette décision ; un magasin existant avec d'autres
paramètres exige `FORCER_CATALOGUE <- TRUE` (il sert tous les profils).

```{r catalogue}
ANS_CHOISIES  <- ANS_HISTORIQUE        # <- À ÉDITER (ex. 22:26) ; défaut = périmètre du profil (démo : millésimes de la base)
ETBS_CHOISIS  <- TYPES_ETBS_LONGS      # <- À ÉDITER (ex. "CHR/U")
etape_catalogue(ans = ANS_CHOISIES, etbs = ETBS_CHOISIS)
cat(lire_si_present(FICHIER_RAPPORT_EXTRACTION(), "etape_catalogue()"), sep = "\n")   # 20_catalogue/rapport_extraction.txt (date en première ligne)
```

Contrôle de couverture avant / après seuil : DP distincts des partiels agrégés (conversion E669
appliquée avant comparaison) vs DP du catalogue ; liste des DP perdus au seuil, triée par effectif.
Chemins absolus (`DIR_PARTIELS`, `DIR_REFERENCES`, magasins partagés).

```{r couverture_dp}
fichiers <- file.path(DIR_PARTIELS, nom_partiel(rep(ETBS_CHOISIS, each = length(ANS_CHOISIES)), rep(ANS_CHOISIES, times = length(ETBS_CHOISIS))))
manquants <- basename(fichiers[!file.exists(fichiers)])
dist_e660 <- lire_si_present(file.path(DIR_REFERENCES, nom_ref("ref_distribution_e660")), "etape_refs()")
if (length(manquants)) cat("partiels absents :", paste(manquants, collapse = ", "), "— produits par etape_partiels_longs(), pas encore exécutée pour ce périmètre (voir etat_pipeline()).\n") else {
  piv <- agreger_partiels(fichiers, c("diag2", "cage", "sexe"), "n")
  if (CONVERSION_E669 && !is.null(dist_e660)) piv <- convertir_e669_comptes(piv, "diag2", c("cage", "sexe"), "n", dist_e660, BARE_E669_DEFAUT)
  avant <- piv |> dplyr::summarise(n = sum(n), .by = diag2)
  apres <- unique(lire_catalogue(DIR_CATALOGUE(), MONO_CATALOGUE(), colonnes = "diag2")$diag2)   # stop actionnable si le catalogue manque
  perdus <- avant[!avant$diag2 %in% apres, ] |> dplyr::arrange(dplyr::desc(n))
  cat("DP distincts avant seuil :", nrow(avant), "; après :", length(apres), "; perdus :", nrow(perdus), "\n"); print(head(perdus, 30))
}
```

## 3.2b Production : repartitionner + typer le catalogue (une fois, sans base)

Le catalogue de production (21,6 M lignes) ne doit JAMAIS être reconstruit sur ce périmètre.
Parts par lettre de DP + DPEC/TPEC (`referentiels/typologie_sejours.yaml`, version tracée) ;
monofichier renommé `.ancien`. Restart R avant.

```{r repartitionner}
etape_repartitionner_catalogue()
memoire_session()
```

## 3.3 Sélection de campagne

`quota_dp_fixe` (production) : k = `NB_LIGNES_PAR_DP` lignes distinctes par DP, variantes
déduites, par population, plafonds `PLAFONDS_DPEC`. `NB_CRH_CIBLE` est un ordre de grandeur.
Les sections 3.3 à 3.7 sont la version courte du cycle de campagne ; la version complète (ouverture de
campagne, registre, palier gardé, carte des fichiers) est `RUN_aval.Rmd`. En mode démo, elles sont
sautées (`demo=FALSE`) : dérouler `RUN_aval.Rmd` à la suite.

```{r selection, demo=FALSE}
etape_selection_longs()    # diagnostic : quota_dp, 1 000 ; production : quota_dp_fixe, NB_CRH_CIBLE
```

## 3.3b Palier 100 k : mesurer le débit

```{r palier_100k, demo=FALSE}
# Palier de MESURE (optionnel) : voir RUN_aval.Rmd §5 (chunks gardés : ne tire que si la surcharge palier est active).
writeLines(c("NB_CRH_CIBLE <- 100000L", "PALIER_ACTIF <- TRUE"), "palier.R"); Sys.setenv(SCENARIOS_PMSI_SURCHARGE = normalizePath("palier.R"))
source("tirage.R")   # session (ETAPES_SEULEMENT) ; la bannière du tirage affiche la surcharge active
if (palier_actif()) { etape_selection_longs(); etape_tirage_das_longs() } else cat("palier non actif : rien exécuté\n")   # débit (scénarios/s) imprimé par chunk
# extrapolation : temps campagne ≈ volume_attendu / débit ; sessions parallèles ≈ ceiling(temps / durée acceptable)
```

## 3.4 Tirage DAS par chunks (reprise : relancer tel quel ; parallélisme par plages)

```{r tirage_das, demo=FALSE}
etape_tirage_das_longs()                                                  # tout
# etape_tirage_das_longs(chunk_range = c(1, 10), populations = "adulte")  # une session par plage disjointe
```

## 3.5 Habillage admin (flux par lots)

```{r habillage, demo=FALSE}
etape_habillage_longs()
```

## 3.6 Fichiers définitifs, rapport, revue

```{r finalisation, demo=FALSE}
etape_finalisation()
etat_pipeline()
```

**Critères** : rapport « TOTAL anomalies = 0 » (dont ^E669 résiduels) ; distribution du nombre
de DAS par classe d'âge conforme aux cibles ; revue humaine de `echantillon_revue.csv`.

```{r revue, demo=FALSE}
revue <- lire_si_present(FICHIER_REVUE(), "etape_finalisation()"); if (!is.null(revue)) View(revue)   # <profil>/60_export_final/echantillon_revue_<C>.csv
top30 <- lire_si_present(FICHIER_TOP30(), "etape_finalisation()"); if (!is.null(top30)) View(top30)
```

## 3.7 Changement de budget / mode (geste destructeur, gardé)

Changer le budget change `NB_VARIANTES` : le garde-fou `selection/_meta.yaml` refuse de mélanger des
chunks tirés sous d'autres paramètres. Vider le dossier de campagne `40_campagnes/<CAMPAGNE>/`
(sélection, chunks, habillé) avant de rejouer 3.3-3.6 — ou ouvrir une autre campagne.

```{r vider_chunks, demo=FALSE}
# Ce que ce chunk touche (SUPPRIME) : <profil>/40_campagnes/<CAMPAGNE>/ (sélection, chunks longs, habillé) de la campagne courante.
# Ce qu'il ne touche JAMAIS : 50_registre/, les livrables 60_export_final/, les magasins partagés (partiels, refs, catalogue, courts).
JE_CONFIRME <- FALSE   # passer à TRUE en connaissance de cause
if (JE_CONFIRME) {
  unlink(DIR_CAMPAGNE(), recursive = TRUE); cat("Vidé :", DIR_CAMPAGNE(), "\n")
} else cat("Rien fait : passer JE_CONFIRME à TRUE d'abord.\n")
```

---

# Règles de cache (mémo)

- Arborescence par étapes sous `PATH_RESULTS` : magasins **partagés** entre profils (`00_partiels/`,
  `10_references/`, `20_catalogue/`, `30_courts/`, `90_diagnostics/`) et dossiers **par profil**
  (`<profil>/40_campagnes/<C>/`, `50_registre/`, `60_export_final/`). Chaque magasin partagé porte un
  `_meta.yaml` avec les paramètres qui le définissent, vérifié à chaque chargement : écart ⇒ stop
  nommant les clés et les issues (régénérer avec `FORCER_*` — il sert tous les profils — ou détourner
  son chemin pour ce profil via `CHEMINS_SURCHARGES`).
- `00_partiels/` : clés bloquantes `K_GRAINE_LONGS`, `NBDA_MAX`, `DUREE_LONGS`, `PIVOTS_LONGS` ;
  **pas** de `SEUIL_PIVOT`, du périmètre d'années ni de `CONVERSION_E669` (`FORCER_PARTIELS`).
- `10_references/` : `AN_REF`, `SEUIL_REF_*`, `CONVERSION_E669`, `BARE_E669_DEFAUT` (`FORCER_REFS`).
- `20_catalogue/` : périmètre + seuil + conversion + clés amont (+ typologie, recette d'id) ;
  identique ⇒ sauté, en écart ⇒ `FORCER_CATALOGUE`. `30_courts/` : `AN_REF`, paramètres courts (`FORCER_COURTS`).
- `40_campagnes/<C>/` (sélection, chunks, habillé, `selection/_meta.yaml`) : reprise telle quelle ; un
  dossier par campagne, à vider après changement de `MODE_SELECTION`, `NB_CRH_CIBLE`,
  `NB_LIGNES_PAR_DP`, `QUOTA_MIN_PAR_UNITE`, `SEED`, de la version de typologie ou du catalogue.
- Hygiène mémoire : `memoire_session()` ; **Restart R avant chaque étape lourde** (le RSS de R
  ne redescend pas après `gc()` ; l'état utile est sur disque, `etat_pipeline()` réoriente).
- Chunking dynamique : `taille_chunk(n) = max(CHUNK_SIZE_MIN, ceiling(n / NB_CHUNKS_MAX))`
  (au plus 50 chunks par tirage, plancher 500 ; `CHUNK_SIZE_FIXE` pour imposer une taille).
  Sidecar `<prefixe>_chunks_meta.yaml` par dossier : reprise acceptée uniquement avec les mêmes
  n / chunk_size / seed_base ; changer `NB_CHUNKS_MAX`, `CHUNK_SIZE_MIN`, `CHUNK_SIZE_FIXE` ou le
  volume d'entrée ⇒ vider les dossiers de chunks (le garde-fou l'impose). Dossiers antérieurs
  au chantier (sans sidecar) : à vider.
- Sessions multiples : les tables temporaires disparaissent à la déconnexion ;
  `etape_prep_data()` recrée à la demande. Ne jamais persister de table en base.

# Tests hors base (et mode démo : `Rscript demo/executer_notebook.R --raz RUN.Rmd` déroule ce notebook sur la base fictive)

```{r tests, demo=FALSE}
system("Rscript tests/test_helpers.R")
system("Rscript tests/test_chaines_sqlite.R")
```
